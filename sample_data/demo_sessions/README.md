# Demo Sessions

`demo_session.csv` is a compact replay sample that mirrors the backend export format.

Use it to:

- exercise the simulator replay mode
- validate frontend chart and replay wiring
- test the backend CSV export/import shape
